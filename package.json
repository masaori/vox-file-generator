{
  "name": "vox-file-generator",
  "version": "1.0.0",
  "description": "Generate .vox file with giving javascript objects.",
  "main": "index.ts",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "lint": "tslint ./*.ts",
    "format": "npx prettier -w ./*.ts"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "@types/buffer-builder": "^0.2.0",
    "@types/node": "^16.11.10",
    "prettier": "^2.5.1",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "buffer-builder": "^0.2.0",
    "date-fns": "^2.27.0",
    "ts-node": "^10.4.0"
  }
}
